{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "routes": [
    { "src": "/storybook/(.*)", "dest": "/storybook/$1" },
    { "src": "/", "status": 301, "headers": { "Location": "/storybook" } }
  ]
}
